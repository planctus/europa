# Name of the project
phing.project.name = Political

# The database url to use for site installs
drupal.db.user =
drupal.db.password =
drupal.db.name =

# Local base URL of the installed site.
base_url = http://political.localhost

server.docroot = /var/www/political/

# Modules to enable
drupal.modules = dt_political_core

# Project folder in MULTISITE SVN
phing.project.svn =
# Project branch in AC
acquia.repo.project.branch = political
