<?php
/**
 * @file
 * DT Policy input install file.
 */

/**
 * Implements hook_install().
 */
function dt_coordination_default_install() {
  // Apply default dt configuration to "Policy input" content type.
  $path = drupal_get_path('module', 'dt_coordination_default') . '/dt_coordination.info';
  $info = drupal_parse_info_file($path);
  drupal_set_message(t('NextEuropa coordination %v feature is installed on your site.', array('%v' => $info['version'])));
}

/**
 * Implements hook_uninstall().
 */
function dt_coordination_default_uninstall() {
  $path = drupal_get_path('module', 'dt_coordination_default') . '/dt_coordination.info';
  $info = drupal_parse_info_file($path);
  drupal_set_message(t('NextEuropa coordination %v feature is uninstalled on your site.', array('%v' => $info['version'])));
}

/**
 * First hook_update for example.
 */
/*
function dt_coordination_default_update_7101() {

}
 */
